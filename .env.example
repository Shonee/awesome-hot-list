# 本地调试时复制为 .env；不要把真实凭证提交到 Git。
HOTLIST_WRITE=1

# 可选。配置后优先读取知乎官方热榜；留空或失效时使用今日热榜降级源。
ZHIHU_COOKIE=

# RSS 渠道。多个 Feed 使用逗号分隔，可写成 名称|URL。
HOTLIST_RSS_FEEDS=
# 每个 RSS 源最多保留 5 条，允许 1-5
HOTLIST_RSS_LIMIT=5

# 仅在手动启用脉脉采集时配置。该渠道接口稳定性较低。
MAIMAI_COOKIE=
