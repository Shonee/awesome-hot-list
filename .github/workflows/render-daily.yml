name: Render Daily Dashboard

on:
  schedule:
    # 北京时间每天 00:40 = UTC 前一天 16:40（晚于各采集任务收尾，确保昨日 CSV 已齐）
    - cron: '40 16 * * *'
  workflow_dispatch:

# 与聚合任务共用串行组，避免同时改动工作树导致 push 冲突
concurrency:
  group: hotlist-repository-writer
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install render dependencies
        run: pip install -r ./src/requirements.txt

      - name: Render current and previous reports
        run: python ./src/script/render.py

      - name: Commit dashboard
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add site/index.html site/data/latest.json site/data/reports/
          if git diff --cached --quiet; then
            echo "no dashboard change"
            exit 0
          fi
          git commit -m "chore: render hotlist dashboard $(date -u +'%Y-%m-%d')"
          git pull --rebase origin "${{ github.event.repository.default_branch }}"
          git push
